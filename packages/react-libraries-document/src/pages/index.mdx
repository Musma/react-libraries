# React Libraries Overview

### pnpm 설치 방법

```
brew install pnpm
```

---

### @musma/react-component 설치 방법

```
pnpm add @musma/react-component
```

---

### @musma/react-icons 설치 방법

```
pnpm add @musma/react-icons
```

---

### @musma/react-utils 설치 방법

```
pnpm add @musma/react-utils
```

---

export default ({ children }) => (
  <article className="prose prose-slate">{children}</article>
);
