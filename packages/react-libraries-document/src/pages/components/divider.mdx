import { Divider } from "@musma/react-component";
import { Box } from "src/components";

# Divider

공통으로 사용 될 Divider 컴포넌트입니다.

### 사용방법

```
import { Divider } from '@musma/react-component'

<Divider />
```

### Horizontal Divider(Default)

<Box className="not-prose">
  <Divider />
</Box>

### Vertical Divider

<Box className="not-prose flex h-[300px] w-full items-center justify-center">
  <Divider orientation="vertical" />
</Box>

export default ({ children }) => (
  <article className="prose prose-lg dark:prose-invert">{children}</article>
);
