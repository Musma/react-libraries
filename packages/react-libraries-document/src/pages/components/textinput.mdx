import { TextInput } from "@musma/react-component";

import { Box } from "src/components";

## TextInput

공통으로 사용 될 TextInput 디자인입니다.

### 사용방법

```
import { TextInput } from '@musma/react-component'

<TextInput />
```

### Type - Text

<Box className="not-prose gap-4">
  <TextInput label="Title" />
  <TextInput label="Title" />
  <TextInput label="Title" />
</Box>

### Type - Password

<Box className="not-prose gap-4">
  <TextInput label="Title" type="password" />
  <TextInput label="Title" type="password" />
  <TextInput label="Title" type="password" />
</Box>

### Type - Search

<Box className="not-prose gap-4">
  <TextInput label="Title" type="search" />
  <TextInput label="Title" type="search" />
  <TextInput label="Title" type="search" />
</Box>

export default ({ children }) => (
  <article className="prose prose-lg dark:prose-invert">{children}</article>
);
