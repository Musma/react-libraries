import { HTMLAttributes } from 'react'

interface TextProps extends HTMLAttributes<HTMLParagraphElement> {
  variant?: 'small'
}

export const Text = ({ variant }: TextProps) => {
  return <div>asopkdasopdk</div>
}
