version: 0.1
applications:
  - appRoot: packages/react-libraries-document
    frontend:
      phases:
        preBuild:
          commands:
            - mv .ci.npmrc .npmrc  
            - nvm use $VERSION_NODE_16
            - npm i -g pnpm
            - pnpm install
        build:
          commands:
            - pnpm build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*